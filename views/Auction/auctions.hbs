<div class="row">
  {{#each auctions}}
  <div class="col-md-4 mb-5">
    <div class="card" style="width: 18rem;">
      <div class="card-body">
        <h5 class="card-title">{{auctionOwner}} is selling: {{paintingName}}</h5>
        <p class="card-text">Current Bid: {{activePrice}} by {{winning}}</p>
        <a href="/auction-details/{{auctionID}}" class="btn btn-secondary">More details</a>
      </div>
    </div>
  </div>
  {{/each}}
  <hr />
</div>